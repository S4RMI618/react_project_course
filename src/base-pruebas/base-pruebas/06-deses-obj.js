export const usContext = ({ key, name, age, range = 'Capitán' }) => {
    // console.log( nombre, edad, rango );
    return {
        keyName: key,
        anios: age,
        latlng: {
            lat: 14.1232,
            lng: -12.3232
        }
    }
}